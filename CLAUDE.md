# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

**Greenfield.** The repo currently contains only a stub `README.md` ("Create Agent First tools"), a Python `.gitignore`, `LICENSE`, and one initial commit. No source code, package manifest, tests, or build configuration exist yet.

When starting new work here, the first implementation decisions (package layout, dependency manager, CLI framework, test runner) have not been made — confirm them with the user before scaffolding, rather than assuming.

## Intended Purpose

**afi-cli** = *Agent First Interface CLI*. A scaffolding tool that generates **all three interface surfaces** an AI agent may need to consume a tool, from a single source of truth:

1. **CLI** — with a built-in `learn` affordance so an agent can introspect the tool and author its own skill/usage notes (rather than relying on `--help` text alone).
2. **MCP server** — exposing a deliberately **minimal menu** of tools (surface area tuned for agent consumption, not maximal API coverage).
3. **HTTP site** — serving markdown pages plus a sitemap, so any agent with a fetch tool can navigate the docs without a bespoke client.

The "Agent First" framing is not just protocol breadth — it is **opinionated best-practice guidance baked into each generated interface** (learnability on CLI, minimalism on MCP, discoverability on HTTP). When proposing structure, optimize for agent ergonomics (discoverability, introspection, low menu cardinality) over human-UI conventions.

afi-cli is expected to **dogfood itself**: its own CLI/MCP/HTTP surfaces should be generated by afi-cli once bootstrapped. Design internal abstractions with that self-hosting round-trip in mind.

## Workspace Conventions (inherited)

This project lives under `/home/spark/git/`, whose workspace-level `CLAUDE.md` establishes defaults that apply unless this file overrides them:

- Python projects in this workspace default to **`uv`** for dependency management (`uv venv && uv pip install -e ".[dev]"`, `uv run pytest`).
- Lint stack when Python code exists: `flake8`, `pylint`, `bandit -r src/`, `black`, `isort`.
- Markdown lint: `markdownlint-cli2 "path/to/file.md"` (with `--fix` for auto-fix). Global config at `~/.markdownlint-cli2.yaml`.

Do not add these tools preemptively — wire them in when there is code to lint.

## Permissions

`.claude/settings.local.json` pre-allows `Bash(python3 *)`. Extend this file (not the global settings) when adding project-local tool permissions.
